extends Control


# Called when the node enters the scene tree for the first time.
func _ready():
	#var card = Card.new(1,"A")
	#$Grid.add_child(card)
	#var card2 = Card.new(2,"A")
	#$Grid.add_child(card2)
	
	pass 


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
